import Foundation
import SwiftUI
import Combine

class SettingsViewModel: ObservableObject {
    // Empty class for now as we've removed language functionality
    // Will be expanded in the future for additional settings
}
